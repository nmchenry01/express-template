/*
  Purpose:
    - This is where we'll put unit tests for the models
*/

describe('models', () => {
  describe('todo.js', () => {
    it('creates a todo model', async () => {
      expect.assertions(0);
    });
  });
});
