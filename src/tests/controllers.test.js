/*
  Purpose:
    - This is where we'll put unit tests for the controllers
*/

describe('controllers', () => {
  describe('todoController.js', () => {
    describe('getTodo', () => {
      it('calls the getTodo controller', async () => {
        expect.assertions(0);
      });
    });
  });
});
