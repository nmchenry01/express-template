/*
  Purpose:
    - This is where we'll put unit tests for the services
*/

describe('services', () => {
  describe('todoService.js', () => {
    describe('createTodo', () => {
      it('calls the createTodo service', async () => {
        expect.assertions(0);
      });
    });
  });
});
